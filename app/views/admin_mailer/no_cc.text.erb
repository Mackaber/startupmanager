<%= @organization %> (<%= @organization.organization_members.count %> miembros, <%= @organization.projects.count %> proyectos)

Tiene adeudo pero no tiene datos de pago

Administradores:

<% @organization.organization_members.where(:level => "Admin").each do |organization_member| %>
  <%= organization_member.user.email %>
<% end %>