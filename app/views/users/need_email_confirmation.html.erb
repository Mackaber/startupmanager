<div id="content">
  <h1>Please check your email</h1>

  <div class="text-block">
    <p>We've sent an email to <%= @user.email %> that contains a link to activate your account. Be sure to check your
      SPAM folder if you don't receive it.
    </p>

    <p>
      <%= form_for(@user, :url => user_confirmation_path(@user), :html => {:method => :post}) do |f| %>
        <%= f.hidden_field :email, :value => @user.email %>
      <div><br/>
        Still not seeing it?
        <a href="#" id="sign_up_resend_confirmation">Resend the email</a>
      </div>
    <% end %>
    </p>
  </div>
</div>
